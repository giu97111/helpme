/// Logo Scudo nel bundle: [kAppLogoAsset] (`logo.jpg`). Notifiche mostrate dall’app
/// (foreground) usano questo file; il server FCM può solo aggiungere un URL se serve.
const String kAppLogoAsset = 'assets/logo.jpg';
